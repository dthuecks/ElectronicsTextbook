\begin{circuitikz}
    \draw (0,0)     node[op amp, yscale=-1] (opamp) {}
    (opamp.+)       to[short, -o] ++(-1,0)
                    node[left]{$V_\text{in}$}
    (opamp.-)       to[short] ++(-.5,0)
                    to[short, *-*] ++(0,-1) coordinate(tmp)
                    to[R, l_=$R_2$, i<^=$I_2$] (tmp -| opamp.out)
                    to[short, -*] (opamp.out)
    (tmp)           to[R, l_=$R_1$, i_>=$I_1$] ++ (0,-2)
                    node[ground] {}
    (opamp.out)     to[short,-o] ++(1,0)
                    node[right] {$V_\text{out}$};
                    
\end{circuitikz}