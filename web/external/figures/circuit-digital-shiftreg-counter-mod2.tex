\begin{circuitikz}
    \tikzset{
        Dflipflop falling/.style={flipflop,
        flipflop def={t1=$C$, t3=$D$, t6=$Q$, t4={\ctikztextnot{$Q$}},
        c1=1, n1=1}},
        %
        add async PC/.style={flipflop def={%
            tu={\ctikztextnot{\texttt{PRE}}}, td={\ctikztextnot{\texttt{CLR}}}}},
    }

    % Placement of digital components
    \draw(0, 0)                 coordinate(start)
                                node[Dflipflop falling](D0){}
    
    % Internal connections
    (D0.pin 3)                  to[short] ++(0,-.75)
                                to[short] (\tikztostart -| D0.pin 4)
                                to[short] (D0.pin 4)

    % Inputs
    (D0.pin 1)                  to[short] ++(-0.5,0)
                                coordinate(C)
                                node[left](){CLK}
    
    % Outputs
    (D0.pin 6)                  to[short] ++(0.5,0)
                                node[right](){$Q_0$}
    
;
\end{circuitikz}