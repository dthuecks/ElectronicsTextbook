\begin{circuitikz}
    \tikzset{Dlatch/.style={flipflop,
        flipflop def={t1=$D$, t3=EN, t6=$Q$, t4={\ctikztextnot{$Q$}},
    }}}

    % Placement of digital components
    \draw(0, 0)             coordinate(start)
                            node[Dlatch, align=center](D1){Latch\\ 1}
    ([xshift=3cm] start)    node[Dlatch, align=center](D2){Latch\\ 2}
    ([xshift=-2.5cm, yshift=-2cm] start)       
                            node[not port](not1){}
    (not1 -| D1)            node[not port](not2){}

    % Internal connections
    (D1.pin 6)              to[short] (D2.pin 1)
    (not1.out)              to[short] (not2.in)
    (D1.pin 3)              to[short,-*] (\tikztostart |- not2.in)
    (not2.out)              to[short] (\tikztostart -| D2.pin 3)
                            to[short] (D2.pin 3)

    % Inputs
    (D1.pin 1)              to[short] (\tikztostart -| not1.in)
                            node[left](){$D$}
    (not1.in)               node[left](){CLK}

    % Outputs
    (D2.pin 6)              to[short] ++(0.5,0)
                            coordinate(Q)
                            node[right](){$Q$}
    (D2.pin 4)              to[short] (\tikztostart -| Q)
                            node[right](){$\overline{Q}$}
;
\end{circuitikz}